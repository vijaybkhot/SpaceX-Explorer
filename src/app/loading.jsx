export default function Loading() {
  return (
    <div style={{ textAlign: "center", marginTop: "4rem" }}>
      <h2>🚀 Loading SpaceX Data...</h2>
      <p>Please wait while we fetch the latest data.</p>
    </div>
  );
}
